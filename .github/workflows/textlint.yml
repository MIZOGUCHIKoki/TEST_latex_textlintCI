name: run-textlint
on: 
  push:
    branches:
      - main
  pull_request:
    paths:
      - '**/**.tex'
  workflow_dispatch:
  
env:
  GITHUB_TOKEN: ${{ secrets.ACCESS_TOKEN }}
jobs:
  run-textlint:
    uses: MIZOGUCHIKoki/ConfigFiles/.github/workflows/textlint.yml@main
    with:
      i_token: ${{ secrets.GITHUB_TOKEN }}
